const { factors } = require("./session5/factors");
const { repetitionMap } = require("./session5/repetitionMap");
const { toBinary } = require("./session5/toBinary");
const { wordLengthInfo, getWordsInStr } = require("./session5/wordLengthInfo");

module.exports = {
  factors,
  repetitionMap,
  toBinary,
  wordLengthInfo,
  getWordsInStr,
};
